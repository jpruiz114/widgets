describe(
    "example",
    function() {
        beforeEach(
            function() {

            }
        );

        afterEach(
            function() {

            }
        );

        it(
            "should fail",
            function() {
                expect(true).to.be.false;
            }
        );
    }
);
