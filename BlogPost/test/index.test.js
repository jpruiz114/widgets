describe(
    "index.js",
    function() {
        beforeEach(
            function() {

            }
        );

        afterEach(
            function() {

            }
        );

        it(
            "should initialize with a given language code and profile id",
            function() {
                var languageCode = "";

                var profileId = "";

                expect(true).to.be.true;
            }
        );
    }
);
